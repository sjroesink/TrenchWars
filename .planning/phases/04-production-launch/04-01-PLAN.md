---
phase: 04-production-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/public/sprites/ships/warbird/*.png
  - packages/client/public/sprites/ships/javelin/*.png
  - packages/client/public/sprites/ships/spider/*.png
  - packages/client/src/sprites/ship-sprites.ts
  - packages/client/src/renderer.ts
  - packages/client/src/remote-player.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Local player ship renders as a sprite PNG instead of a green triangle"
    - "Remote player ships render as sprite PNGs instead of red triangles"
    - "Ship sprites rotate by frame lookup (40 frames per ship) not PixiJS rotation"
    - "All three ship types (Warbird, Javelin, Spider) render with correct sprites"
  artifacts:
    - path: "packages/client/src/sprites/ship-sprites.ts"
      provides: "Sprite sheet loader and frame lookup"
      exports: ["ShipSpriteManager"]
    - path: "packages/client/public/sprites/ships/"
      provides: "Ship sprite PNGs organized by type"
  key_links:
    - from: "packages/client/src/renderer.ts"
      to: "packages/client/src/sprites/ship-sprites.ts"
      via: "ShipSpriteManager import"
      pattern: "ShipSpriteManager"
    - from: "packages/client/src/remote-player.ts"
      to: "packages/client/src/sprites/ship-sprites.ts"
      via: "ShipSpriteManager import"
      pattern: "ShipSpriteManager"
---

<objective>
Replace programmatic Graphics triangles with real SubSpace ship sprites for both local and remote players.

Purpose: The game currently renders ships as simple colored triangles. Real ship sprites from SubSpace make the game visually authentic and production-ready for the public URL.
Output: Ship sprite assets in public directory, sprite manager module, updated renderer and remote-player to use sprite-based rendering with rotation frame lookup.
</objective>

<execution_context>
@C:/Users/sjroe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sjroe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/client/src/renderer.ts
@packages/client/src/remote-player.ts
@packages/client/src/game-loop.ts
@packages/client/src/interpolation.ts
@packages/client/src/main.ts

<interfaces>
<!-- Key types the executor needs -->

From packages/shared/src/types.ts:
```typescript
export interface InterpolatedEntity {
  x: number; y: number; vx: number; vy: number;
  orientation: number; // 0-1 normalized (0=up, 0.25=right, 0.5=down, 0.75=left)
  energy: number; shipType: number; alive: boolean;
}
```

Ship type numbers: Warbird=0, Javelin=1, Spider=2

Sprite source: D:\Projects\bm2\output\
- ships/ -- 320 PNGs (8 ships x 40 frames, 144x144px). Index: ship_number * 40 + rotation_frame
- wbroll/ -- Warbird rotation (40 frames, wbroll_0.png to wbroll_39.png)
- jvroll/ -- Javelin rotation (40 frames)
- sproll/ -- Spider rotation (40 frames)
- Frame 0=up, 10=right, 20=down, 30=left (9 degree increments, clockwise)

Orientation mapping: orientation (0-1) -> frame = Math.round(orientation * 40) % 40
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy sprites and create ShipSpriteManager</name>
  <files>
    packages/client/public/sprites/ships/warbird/,
    packages/client/public/sprites/ships/javelin/,
    packages/client/public/sprites/ships/spider/,
    packages/client/src/sprites/ship-sprites.ts
  </files>
  <read_first>
    packages/client/src/renderer.ts,
    packages/client/src/remote-player.ts,
    packages/client/src/main.ts
  </read_first>
  <action>
1. Copy sprite PNGs from D:\Projects\bm2\output\ into client public directory:
   - Copy wbroll/wbroll_0.png through wbroll_39.png -> packages/client/public/sprites/ships/warbird/0.png through 39.png
   - Copy jvroll/jvroll_0.png through jvroll_39.png -> packages/client/public/sprites/ships/javelin/0.png through 39.png
   - Copy sproll/sproll_0.png through sproll_39.png -> packages/client/public/sprites/ships/spider/0.png through 39.png
   (Use the per-ship-type roll directories, NOT the combined ships/ directory, since we only need Warbird/Javelin/Spider)

2. Create packages/client/src/sprites/ship-sprites.ts with class ShipSpriteManager:
   - SHIP_NAMES array: ['warbird', 'javelin', 'spider'] indexed by shipType (0, 1, 2)
   - FRAMES_PER_SHIP = 40
   - async loadAll(): Promise<void> -- loads all 120 textures (3 ships x 40 frames) using PixiJS Assets.load(). Store in a Map<string, Texture> keyed by "{shipName}_{frame}".
   - getTexture(shipType: number, orientation: number): Texture -- converts orientation (0-1 normalized) to frame index: Math.round(orientation * 40) % 40, returns the cached Texture. Fall back to frame 0 if missing.
   - Textures loaded from paths like /sprites/ships/warbird/0.png (relative URLs served by Vite/static server)

Key implementation detail: PixiJS v8 uses `import { Assets, Texture } from 'pixi.js'`. Use Assets.load(url) which returns a Texture. Pre-load all 120 textures in loadAll() so getTexture() is synchronous.
  </action>
  <verify>
    <automated>cd D:/Projects/TrenchWars && ls packages/client/public/sprites/ships/warbird/0.png packages/client/public/sprites/ships/javelin/0.png packages/client/public/sprites/ships/spider/0.png && ls packages/client/public/sprites/ships/warbird/ | wc -l && cat packages/client/src/sprites/ship-sprites.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/public/sprites/ships/warbird/ contains 40 PNGs (0.png through 39.png)
    - packages/client/public/sprites/ships/javelin/ contains 40 PNGs
    - packages/client/public/sprites/ships/spider/ contains 40 PNGs
    - ship-sprites.ts exports ShipSpriteManager class with loadAll() and getTexture(shipType, orientation)
  </acceptance_criteria>
  <done>120 sprite PNGs copied to public directory, ShipSpriteManager module created with load and lookup API</done>
</task>

<task type="auto">
  <name>Task 2: Replace Graphics triangles with sprites in renderer and remote-player</name>
  <files>
    packages/client/src/renderer.ts,
    packages/client/src/remote-player.ts,
    packages/client/src/main.ts
  </files>
  <read_first>
    packages/client/src/renderer.ts,
    packages/client/src/remote-player.ts,
    packages/client/src/main.ts,
    packages/client/src/sprites/ship-sprites.ts,
    packages/client/src/game-loop.ts
  </read_first>
  <action>
1. Update packages/client/src/renderer.ts:
   - Import ShipSpriteManager and PixiJS Sprite
   - Add shipSpriteManager field and shipType field to Renderer class
   - In init(): accept shipSpriteManager param (already loaded). Replace the Graphics triangle:
     - Remove: `this.shipGraphics = new Graphics(); this.shipGraphics.poly(...); this.shipGraphics.fill(...);`
     - Replace with: `this.shipSprite = new Sprite()` added to shipContainer. Set anchor to 0.5, 0.5 (center). Set initial texture from spriteManager.
   - In render(): instead of `this.shipGraphics.rotation = state.orientation * Math.PI * 2`, do:
     `this.shipSprite.texture = this.shipSpriteManager.getTexture(this.shipType, state.orientation);`
     (No rotation -- the sprite frame IS the rotation)
   - Add setShipType(type: number) method so main.ts can set it after ship selection.

2. Update packages/client/src/remote-player.ts:
   - Import ShipSpriteManager and PixiJS Sprite
   - Constructor takes shipSpriteManager as parameter, stores it
   - Replace Graphics pool with Sprite pool:
     - getOrCreateGraphics -> getOrCreateSprite: creates a new Sprite() with anchor 0.5, 0.5
     - In render(): for each entity, instead of setting gfx.rotation, set:
       `sprite.texture = this.shipSpriteManager.getTexture(entity.shipType, entity.orientation);`
     - No rotation assignment needed -- frame lookup handles it
   - Update types: pool is Sprite[] not Graphics[]

3. Update packages/client/src/main.ts:
   - Import ShipSpriteManager
   - After map loads and before renderer.init(), create and await shipSpriteManager.loadAll()
   - Pass shipSpriteManager to renderer.init() and to RemotePlayerRenderer constructor (via renderer)
   - Set renderer.setShipType(selectedShipType) after ship selection

4. Ensure the glow filters still work with Sprite children in Containers (they do -- GlowFilter applies to any DisplayObject).

5. Update Dockerfile if needed: the sprites are in packages/client/public/ which gets built into dist/ by Vite, so they should be automatically included. Verify the Vite build includes the public/ directory contents.
  </action>
  <verify>
    <automated>cd D:/Projects/TrenchWars && npx -w packages/client tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - renderer.ts uses Sprite instead of Graphics for local ship
    - renderer.ts sets texture from ShipSpriteManager.getTexture() instead of rotation
    - remote-player.ts uses Sprite pool instead of Graphics pool
    - remote-player.ts sets texture per entity.shipType and entity.orientation
    - main.ts loads ShipSpriteManager before renderer.init()
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Local and remote ships render as sprite PNGs with rotation frame lookup, no Graphics triangles remain for ship rendering</done>
</task>

</tasks>

<verification>
- `npx -w packages/client tsc --noEmit` passes with no type errors
- `cd packages/client && npx vite build --outDir dist` completes successfully (sprites included in build)
- Visual: opening the game in browser shows sprite ships instead of triangles
</verification>

<success_criteria>
- All ship rendering uses PNG sprites with 40-frame rotation lookup
- No programmatic Graphics triangles remain for ship rendering (local or remote)
- Three ship types each render with their own sprite set
- Build succeeds and sprites are included in production bundle
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-launch/04-01-SUMMARY.md`
</output>
