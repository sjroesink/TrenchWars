---
phase: 03-game-experience
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - packages/client/src/main.ts
  - packages/client/src/game-loop.ts
  - packages/client/src/ship-select.ts
  - packages/client/src/ui/game-over.ts
  - packages/shared/src/types.ts
  - packages/shared/src/game-modes.ts
autonomous: false
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - UIEX-01
  - UIEX-02
  - UIEX-06
  - UIEX-07

must_haves:
  truths:
    - "Sound effects play for thrust, bullet fire, bomb fire, explosions, bounce, and death"
    - "Engine exhaust particles appear while thrusting"
    - "Audio initializes on first user interaction in ship select"
    - "Game-over screen shows winner and auto-dismisses"
    - "Complete flow works: ship select -> play -> HUD/radar/chat/scoreboard all visible -> death/respawn -> game over"
  artifacts:
    - path: "packages/client/src/ui/game-over.ts"
      provides: "Game-over overlay screen"
      exports: ["GameOverScreen"]
  key_links:
    - from: "packages/client/src/main.ts"
      to: "packages/client/src/audio/sound-manager.ts"
      via: "SoundManager initialized on ship-select interaction, play() called from game events"
      pattern: "soundManager\\.play|soundManager\\.init"
    - from: "packages/client/src/game-loop.ts"
      to: "packages/client/src/renderer.ts"
      via: "spawnExhaust called while thrusting, renderEffects called each frame"
      pattern: "spawnExhaust|renderEffects"
    - from: "packages/client/src/main.ts"
      to: "packages/client/src/ui/game-over.ts"
      via: "Game-over screen shown on GAME_STATE game-over event"
      pattern: "gameOverScreen\\.show"
---

<objective>
Wire all Phase 3 systems together: connect audio to game events, exhaust to thrust, game-over screen, and verify the complete game experience end-to-end.

Purpose: Individual components (HUD, radar, chat, audio, effects) need to be wired into the game loop and event flow to create a cohesive experience.
Output: Fully integrated game experience with audio, visual effects, game-over flow, and human verification.
</objective>

<execution_context>
@C:/Users/sjroe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sjroe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-game-experience/03-RESEARCH.md
@.planning/phases/03-game-experience/03-UI-SPEC.md
@.planning/phases/03-game-experience/03-01-SUMMARY.md
@.planning/phases/03-game-experience/03-02-SUMMARY.md
@.planning/phases/03-game-experience/03-03-SUMMARY.md
@.planning/phases/03-game-experience/03-04-SUMMARY.md

@packages/client/src/main.ts
@packages/client/src/game-loop.ts
@packages/client/src/renderer.ts
@packages/client/src/input.ts
@packages/client/src/ship-select.ts
@packages/client/src/audio/sound-manager.ts
@packages/client/src/visual-effects.ts
@packages/client/src/network.ts
@packages/shared/src/protocol.ts
@packages/shared/src/game-modes.ts

<interfaces>
From packages/client/src/audio/sound-manager.ts (Plan 04):
```typescript
export class SoundManager {
  init(): void;
  play(name: string, pan?: number): void;
  startThrust(): void;
  stopThrust(): void;
  isInitialized(): boolean;
}
```

From packages/client/src/visual-effects.ts (Plan 04):
```typescript
export class VisualEffects {
  spawnExhaust(shipX: number, shipY: number, orientation: number): void;
  addExplosion(x: number, y: number, large: boolean): void;
  render(camera: Camera): void;
}
```

From packages/client/src/renderer.ts (Plan 04):
```typescript
export class Renderer {
  spawnExhaust(shipX: number, shipY: number, orientation: number): void;
  renderEffects(camera: Camera): void;
  addExplosion(x: number, y: number): void;
}
```

From packages/shared/src/game-modes.ts (Plan 01):
```typescript
export type GameModeState = FFAState | TeamArenaState;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Game-over screen, audio wiring, exhaust integration, and full event flow</name>
  <files>
    packages/client/src/ui/game-over.ts,
    packages/client/src/main.ts,
    packages/client/src/game-loop.ts,
    packages/client/src/ship-select.ts
  </files>
  <read_first>
    packages/client/src/main.ts,
    packages/client/src/game-loop.ts,
    packages/client/src/ship-select.ts,
    packages/client/src/input.ts,
    packages/client/src/network.ts,
    packages/client/src/renderer.ts,
    packages/client/src/audio/sound-manager.ts,
    packages/client/src/visual-effects.ts,
    .planning/phases/03-game-experience/03-UI-SPEC.md,
    packages/shared/src/protocol.ts
  </read_first>
  <action>
    1. Create packages/client/src/ui/game-over.ts -- GameOverScreen class:
       - Overlay div `#game-over` centered on screen, `position: fixed; z-index: 1000; display: none;`
       - Background: `rgba(0,0,0,0.85)`
       - Winner text div: 28px semibold centered
       - Round info div: 14px `#888888`
       - `show(text: string, color: string, subtitle?: string): void`:
         - Set winner text content and color
         - Set subtitle if provided
         - Show overlay
         - Auto-dismiss after 5000ms (hide overlay)
       - `hide(): void` -- hides overlay
       - `destroy(): void`
       - All text uses `font-family: monospace`

    2. Update packages/client/src/ship-select.ts:
       - Accept optional `onInteraction?: () => void` callback in constructor
       - In both the click handler and the keydown handler, before resolving, call `this.onInteraction?.()` ONCE (use a flag to prevent double-call)
       - This is the hook for SoundManager.init() -- browser autoplay policy requires user gesture

    3. Update packages/client/src/game-loop.ts:
       - Add optional `soundManager?: SoundManager` to GameLoopOptions, store reference
       - Add optional `onExhaust?: (x: number, y: number, orientation: number) => void` callback to options
       - Track previous thrust state: `private wasThrusting = false;`
       - In the fixed timestep section, after polling input:
         - If `input.thrust && !this.wasThrusting && this.soundManager`: call `this.soundManager.startThrust()`
         - If `!input.thrust && this.wasThrusting && this.soundManager`: call `this.soundManager.stopThrust()`
         - `this.wasThrusting = input.thrust;`
       - In the render section, if input.thrust: call `this.renderer.spawnExhaust(this.shipState.x, this.shipState.y, this.shipState.orientation)` each frame
       - After calling renderer.render(), call `this.renderer.renderEffects(this.camera)`

    4. Update packages/client/src/main.ts -- wire everything together:
       - Import SoundManager, GameOverScreen
       - Create SoundManager instance: `const soundManager = new SoundManager();`
       - Pass `onInteraction: () => soundManager.init()` to ShipSelectOverlay constructor
       - Pass soundManager to GameLoop constructor
       - Create GameOverScreen: `const gameOverScreen = new GameOverScreen();`

       Audio triggers in network handlers:
       - `onDeath`: call `soundManager.play('explosion')` for any death. If local player died: also call `soundManager.play('death')`
       - In game loop weapon input section: when fire is true, play `soundManager.play('bulletFire')`. When fireBomb is true, play `soundManager.play('bombFire')`
         - This requires passing soundManager to game loop or having the main.ts handle it via a callback
         - Simplest: in main.ts, wrap the existing game loop or add hooks. Since GameLoop already handles input, add an `onFire` callback option to GameLoopOptions:
           `onFire?: (type: 'bullet' | 'bomb') => void`
         - In GameLoop, when weapons.fire is true and being sent, call `this.onFire?.('bullet')`
         - When weapons.fireBomb is true, call `this.onFire?.('bomb')`
         - In main.ts: `onFire: (type) => soundManager.play(type === 'bullet' ? 'bulletFire' : 'bombFire')`
       - Wall bounce sound: Add to GameLoop -- detect when ship velocity sign changes after wall collision by comparing vx/vy before and after physics. If magnitude decreased significantly (bounce detected), call `soundManager.play('bounce')`. Simpler approach: track position before/after collision, if velocity reversed on any axis, play bounce.
         - Actually simpler: in the game loop physics section, save vx/vy before applyWallCollision, compare after. If sign flipped on either axis: `soundManager?.play('bounce')`

       Game-over handler:
       - In `onGameState` handler (from network): check `event === 'game-over'`
       - Extract winner info from data, show game-over screen:
         `gameOverScreen.show(winnerText, winnerColor, subtitle)`
       - FFA: "{name} wins the match!" in winner's ship color
       - Team: "{Team color} team wins!" in team color (`#4488ff` or `#ff4444`)

    5. Snapshot name tracking:
       - Ensure GameSnapshot includes `name` and `kills` and `deaths` fields. If Plan 01 added `name` to snapshot player data, use it. Otherwise, use the playerNames Map from Plan 02 for name resolution.
       - Update the GameSnapshot type in packages/shared/src/types.ts if needed: add `name?: string`, `kills?: number`, `deaths?: number` to the snapshot player entry.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p packages/client/tsconfig.json && npx tsc --noEmit -p packages/shared/tsconfig.json && cd packages/server && npx vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class GameOverScreen" packages/client/src/ui/game-over.ts
    - grep -q "soundManager" packages/client/src/main.ts
    - grep -q "soundManager.init" packages/client/src/main.ts
    - grep -q "soundManager.play" packages/client/src/main.ts
    - grep -q "startThrust" packages/client/src/game-loop.ts
    - grep -q "stopThrust" packages/client/src/game-loop.ts
    - grep -q "spawnExhaust" packages/client/src/game-loop.ts
    - grep -q "renderEffects" packages/client/src/game-loop.ts
    - grep -q "onInteraction" packages/client/src/ship-select.ts
    - grep -q "gameOverScreen" packages/client/src/main.ts
    - grep -q "game-over" packages/client/src/main.ts
  </acceptance_criteria>
  <done>
    Audio plays for all game events (thrust, fire, explosion, bounce, death).
    Audio initializes on first ship-select interaction.
    Engine exhaust particles visible while thrusting.
    Visual effects render each frame.
    Game-over screen shows on match end and auto-dismisses.
    All TypeScript compiles. All server tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 3 game experience</name>
  <files>none</files>
  <action>
    Human visual and functional verification of the complete Phase 3 game experience.
    All automation is complete in Task 1. This checkpoint confirms everything works together.
  </action>
  <read_first>
    .planning/phases/03-game-experience/03-UI-SPEC.md
  </read_first>
  <what-built>
    Complete game experience layer: FFA deathmatch with live scoreboard, HUD (energy bar + K/D), kill feed, radar minimap, text chat, neon glow/bloom visual effects, engine exhaust particles, enhanced explosions, audio sound effects, and game-over screen.
  </what-built>
  <how-to-verify>
    1. Start server: `cd packages/server && npx tsx src/index.ts` (or however server starts)
    2. Open client: `cd packages/client && npx vite --port 9010` then browse to http://localhost:9010
    3. Open a SECOND browser tab at http://localhost:9010

    In each tab:
    a. Enter a name, select a ship (press 1, 2, or 3)
    b. Verify: ships have neon glow effect, global bloom visible
    c. Fly around: verify engine exhaust particles appear behind ship while thrusting
    d. Check HUD: energy bar at bottom center (color changes as energy depletes), K/D at bottom left
    e. Check radar: minimap at bottom-right showing your green dot and other player's white dot
    f. Press Tab: scoreboard appears centered with player names, kills, deaths
    g. Release Tab: scoreboard disappears
    h. Press Enter: chat input appears, type a message, press Enter to send
    i. Verify: message appears in both tabs
    j. While chat is open: arrow keys should NOT move the ship
    k. Press Escape: chat closes without sending
    l. Fire bullets (Space) and bombs (F key): verify sound effects play (if audio files exist)
    m. Kill the other player: verify kill feed entry appears at top-right, explosion effect visible
    n. Verify audio: thrust sound while flying, bullet/bomb fire sounds, explosion on death

    If audio files are missing (placeholder), sounds won't play but the game should not crash.
  </how-to-verify>
  <verify>Human visual inspection confirms all UI elements and effects are present and functional</verify>
  <done>All Phase 3 features verified working: HUD, scoreboard, radar, chat, audio, visual effects, game-over flow</done>
  <resume-signal>Type "approved" or describe issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/client/tsconfig.json` -- client compiles
2. `npx tsc --noEmit -p packages/shared/tsconfig.json` -- shared compiles
3. `cd packages/server && npx vitest run -x` -- all server tests pass
4. Human verification of complete game experience
</verification>

<success_criteria>
- All Phase 3 requirements visually confirmed working
- Audio system connected to game events
- Visual effects (glow, bloom, exhaust, explosions) visible
- HUD, scoreboard, radar, chat all functional
- Game-over screen appears on match end
- No TypeScript errors, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-experience/03-05-SUMMARY.md`
</output>
